# Archived site files

Kept rather than deleted, per the repo rule in [CLAUDE.md](../../CLAUDE.md). Nothing here is
built or served. Do not edit these files expecting a change to appear on the site.

| File | Archived | What replaced it |
|---|---|---|
| `single-page-map.html` | 2026-07-26 | The self-contained single-page map, generated by the old `site/build.mjs`. Superseded when the site became a Next.js app: its SVG drawing code lives on unchanged in [`lib/map-engine.js`](../lib/map-engine.js), its styles in [`app/map/map.css`](../app/map/map.css), and its About copy in [`map.config.json`](../map.config.json) under `about`. |
